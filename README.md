# My Laravel Web App

Aplikasi web Laravel untuk manajemen staff.

## Features

- CRUD Staff
- Bootstrap UI
- MySQL Database

## Installation

1. Clone the repository
2. Run `composer install`
3. Run `npm install`
4. Copy `.env.example` to `.env` and configure
5. Run `php artisan migrate`
6. Run `php artisan serve`
